## Phase 0 Code Review (T001-T014)

### Issues Found & Fixed:
- C1: Added missing `immer` dependency (required for Zustand middleware)
- C2: Added strict tsconfig flags (noUncheckedIndexedAccess, noImplicitReturns, noFallthroughCasesInSwitch, forceConsistentCasingInFileNames). Note: skipLibCheck must remain true due to RN/DOM type conflicts.
- C3: Changed App to named export throughout (App.tsx, index.ts)
- C4: Replaced `as string` type assertions in env.ts with Zod schema validation
- C5/C6: Aligned ERPAdapter interface with spec (parseRow, getFieldSchema, getAccountTypes), added comment noting it will move to features/migration/types/
- W1: QueryClient moved inside component via useState for test isolation
- W3: Added console.warn for font loading errors

### Accepted as-is:
- W5: .env files tracked intentionally (no secrets, useful for onboarding)
- W6: tailwindcss@3.3.2 pinned intentionally for NativeWind v4 compat
- S1: theme.ts duplication with tailwind.config.js accepted (needed for imperative JS)
- S3: react-native-toast-message used instead of sonner (sonner is web-only)
